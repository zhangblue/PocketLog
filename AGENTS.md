# 栖账 Web UI 需求

## 产品定位

- 产品名称：栖账。
- 面向个人用户的单用户记账 Web 应用。
- 核心价值：记录日常收支，并提供可追溯的消费洞察。
- 采用“静谧账本”视觉方案：米白背景、深绿色主色、低饱和配色、克制且清晰。
- 桌面端优先，支持宽度不低于 1024px 的布局。

## 技术与数据

- 前端使用 React、TypeScript、Vite 和原生 CSS；后端使用 Rust、Axum、SeaORM 与 PostgreSQL。
- 所有后端代码必须放在 `backend/` 目录中；前端通过 `/api/v1` 与后端交互，不得保留财务 `localStorage` 或本地 repository 作为数据来源。
- 账本数据持久化到 PostgreSQL；不实现远程同步、鉴权、多用户系统、预算、资产管理、订阅管理或账单导入。
- 数据库 schema 只能通过显式 `migrate` 命令变更；`serve` 启动时只校验 schema，绝不自动迁移。
- 运行时数据命令职责固定为：`migrate` 只变更 schema；`init` 按名称幂等补齐十一项预置分类；`demo` 仅在分类已初始化且账本为空时写入演示数据；`clean` 清空业务数据但保留 schema；`serve` 只校验并启动服务。首次初始化或清空后重建演示数据必须按 `migrate → init → demo → serve` 执行，`serve` 不得自动写库。
- 十一项预置分类为：支出类“餐饮、交通、购物、居住、娱乐、通讯、网络、水费、电费”，收入类“工资、其他”。`init` 重复执行不得插入重复分类，`demo` 不得创建预置分类。
- 首次使用提供演示数据；页面至少展示 5 条样例交易。
- 数据加载、保存、删除、恢复和标签迁移失败时，不得让界面状态与持久化数据不一致。
- 损坏或不符合业务约束的本地数据必须安全回退，并提供可恢复的错误与重试反馈。

## 应用结构

- 一级页面包括：总览、收支明细、消费分析、月度报告、分类与账户标签。
- 全局月份采用真实自然月，可切换到已有交易月份。
- 一级页面共享同一份月份、交易、分类、账户、筛选和分析上下文。
- 提供全局“记一笔”入口。

## 总览

- 展示本月支出、本月收入、结余率、日均支出和交易笔数。
- 指标基于当月全部交易，并提供真实的上期比较。
- 日均支出使用当前月份的真实天数。
- 展示真实支出趋势，并提供标题、描述和可见文字摘要。
- 展示分类占比，采用“前四大分类 + 其他”，最多五种颜色。
- 展示自动洞察，所有结论都能下钻到对应明细证据。
- 最近交易只展示 5 条，但汇总必须基于当月全部记录。

## 快捷记账

- 支持支出、收入和账户间转账。
- 支持金额、名称、分类、账户、日期、时间和备注。
- 转账必须选择不同的转出与转入账户，并记录资金来源和去向。
- 默认日期和时间来自用户本地当前时间。
- 金额为空、为零、非数字或其他非法字段时阻止保存，并把焦点移到首个错误字段。
- 保存失败时保留用户输入并显示可恢复错误。
- 普通保存后关闭抽屉；“保存并继续”保留交易类型和日期时间，重置金额、名称和备注。
- 有未保存修改时，取消或按 Escape 必须确认“继续编辑”或“放弃”。
- 保存成功后提供明确的可访问反馈。
- 抽屉打开时限制焦点，关闭后将焦点返回触发按钮。

## 收支明细

- 支持按月份、日期范围、类型、分类和账户筛选。
- 洞察下钻必须显示来源说明，并可继续收紧或一键清除筛选。
- 列表展示交易名称、分类、账户、日期、备注摘要和金额。
- 转账显示“转出账户 → 转入账户”，使用中性符号，不显示为负支出。
- 交易行金额固定显示两位小数，例如 `¥32.00`、`¥12,500.00`。
- 删除成功后提供 5 秒撤销窗口；切换页面不得延长窗口。
- 删除、恢复失败时保持数据一致并提供重试。
- 删除、撤销、到期和连续删除时保持合理的键盘焦点，不得抢走筛选控件或模态框中的焦点。

## 消费分析

- 提供支出趋势、分类构成、分类环比和自动洞察。
- 支持本月、近 3 个月和自定义起止日期，并支持账户筛选。
- 历史不足时明确说明，不生成虚假趋势或增长率。
- 分类占比采用“前四大分类 + 其他”；“其他”可下钻到其包含的全部分类。
- 环比和变化洞察下钻时必须同时提供当前周期与上一周期的完整证据。
- 周末交通洞察必须由真实周末交易及可解释条件生成，并只下钻到对应证据。
- 结余率洞察必须包含收入和支出证据，并排除转账。
- 下钻返回后恢复原有时间范围、账户筛选和滚动位置；滚动恢复只执行一次。
- 所有图表必须可通过键盘操作，并提供标题、描述和可见文字摘要。

## 月度报告

- 展示报告标题、财务评分与上期变化、最大节省、最大增长和消费故事。
- 报告内容必须由真实数据确定性生成，不得与汇总数据矛盾。
- 缺少本期或上期数据时诚实降级。
- 报告亮点或评分可下钻到对应收支明细。
- 提供“导出 PDF”入口，调用浏览器打印。
- 打印使用 A4、16mm 页边距、白色背景，只保留报告内容并隐藏导航、筛选、按钮、浮层和错误提示。
- 打印不可用或失败时提供可访问的错误反馈。

## 分类与账户标签

- 分类支持创建、重命名、排序和停用。
- 未被交易引用的分类可确认后删除。
- 已被引用的分类只能停用，或迁移到同类型启用分类后删除。
- 收入和支出必须分别至少保留一个启用分类。
- 账户标签支持创建、重命名和停用，不展示余额或资产信息。
- 停用标签仍可用于历史交易展示与历史筛选，但不得出现在新交易的可选项中。
- 分类迁移涉及交易和标签两个存储时必须保持一致；任一步失败都不得产生半完成状态。
- 确认对话框必须具备焦点陷阱、Escape 关闭、错误提示和关闭后的焦点归还。
- 标签页签支持规范的键盘方向键、Home、End 和 ARIA 关联。

## 状态与空态

- 首次加载提供局部加载状态。
- 局部面板失败时保留标题与容器，显示失败说明和重试操作。
- 区分整个账本首次使用、当前筛选无结果和历史数据不足。
- 每种空态提供且只提供一个明确主操作。
- 当前月份无交易但其他月份有记录时，不得显示为首次使用。

## 格式、无障碍与响应式

- 人民币使用 `¥` 和千位分隔符。
- 交易行固定两位小数；汇总和分析金额为整数时不显示小数，确有小数时显示两位，不能丢失真实金额。
- 趋势同时使用箭头、百分比和文字，不只依赖颜色。
- 普通文字满足 WCAG AA 4.5:1，对焦指示满足 3:1。
- 所有交互支持键盘，并提供清晰的 `:focus-visible` 样式。
- 模态框打开时背景不可交互，关闭后焦点返回合理位置。
- 支持 `prefers-reduced-motion`。
- 在 1440×900 和 1024×768 下不得出现关键内容裁切或不可控横向溢出。

## 验收与执行约束

- 使用自动化测试覆盖真实 Provider、真实持久化和跨页面交互，不能只断言展示文案。
- `src/domain` 与 `src/app` 的行覆盖率不得低于 90%，覆盖率门禁必须能使不达标构建失败。
- Rust 覆盖率不设本次交付的硬性门禁；仍应为新增或修复的后端逻辑提供适当的单元和 PostgreSQL 集成测试。
- TypeScript 严格检查、完整测试和生产构建必须通过。
- 使用极端时区测试验证日期、月份和周末判断不受宿主时区影响。
- 浏览器操作、双视口验收、键盘检查、控制台检查和打印预览只使用 Chrome DevTools MCP，不使用 Chrome CLI、CDP、curl、Playwright 或其他浏览器替代方案。
- 当前项目不使用 Git 进行版本控制，不运行 Git 命令，不创建提交或分支。
